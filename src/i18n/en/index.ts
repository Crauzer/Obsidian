import { common } from './common';
import { mountedWads } from './mountedWads';
import { route } from './route';
import { settings } from './settings';

export const en = { common, route, mountedWads, settings };
